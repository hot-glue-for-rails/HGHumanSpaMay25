<%= turbo_stream.replace "__#{dom_id(@appointment)}" do %>
  <%= render partial: 'line', locals: {appointment: @appointment,  }   %>
<% end %>
<!-- parental updated -->

<!-- flash notices -->
<%= turbo_stream.update "flash_notices" do %>
  <%= render partial: "layouts/flash_notices", locals: {resource: @appointment} %>
<% end %>
